# GLOSSARY — <PROJECT NAME>

> Every project-specific term or method defined once. One thing, one name — bind aliases.
> No term is used elsewhere in the repo before it appears here.

- **<term>** — <definition>.
- **<term>** — <definition>.
- **<alias> → <canonical term>** — use the canonical name; the alias is bound here only.
