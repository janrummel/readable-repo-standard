# STATE — <PROJECT NAME>

> Single source of truth for the working state. Lives in git, not in the chat.
> Status lives ONLY here; other files do not declare their own completeness.

## The ONE next action
<Exactly one concrete action. Not a list. The thing to do next, unambiguously.>

## Next
<What comes after, briefly. Optional.>

## Done
- <completed item — and, if code, "done" means reproducible from repo inputs>

## Parked (do NOT pick this up as "next")
- <deliberately deferred item + a pointer to the decision below. Distinct from unfinished.>

## Decisions (with why)
- **<decision>** Why: <the reason, so a successor does not overturn it by accident>.

## Open
- <open threads, ambiguities, questions>

## Context
<Origin, links, anything a successor needs that is not obvious from the code.>
