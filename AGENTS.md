# AGENTS.md — bridge for docking agents

This repo follows the Readable Repo Standard (self-explaining repo, memory in git).

Entry point: read `START_HERE.md`, then `STATE.md`. Terms: `GLOSSARY.md`.
Environment and limits: `MANIFEST.md`. The full spec is `STANDARD.md`.

This is a prose + small-example repo. The example under `example/tidymark` runs with
`python -m pytest`.
